# Backend origin the client talks to (API + /gateway WebSocket + /files).
# Dev default (when unset) is http://localhost:3000. Production build reads .env.production.
VITE_SERVER_URL=http://localhost:3000
