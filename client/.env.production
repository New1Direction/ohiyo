# The backend the packaged desktop app connects to.
# This points at the official Ohiyo backend. To self-host instead, deploy the server
# (see DEPLOY.md / GO-LIVE.md) and replace the URL with your own https://<your-app>.fly.dev,
# then rebuild the desktop app.
VITE_SERVER_URL=https://ohiyo.fly.dev
